{
  "name": "trigger-black",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve -l 3000 .",
    "dev:pages": "wrangler pages dev . --compatibility-date=2026-05-04 --ip 127.0.0.1 --port 8788 --do GEMINI_PROXY=GeminiProxy@src/index.js",
    "dev:api": "wrangler dev",
    "deploy:api": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
